Install the matplotlib dependency. On linux this is usually something like:
```
pip3 install matplotlib
```

Run the code:
```
python3 main.py
```

Displays a graph and saves it to `weed_spread.png`.
